You are a senior backend + frontend engineer. I will provide the full UI details of this page:
URL: http://127.0.0.1:8000/admin/categories

Goal: Make this page fully dynamic using APIs.

Your tasks:
A) Identify every dynamic component and user action on the page.
B) Propose a complete API list required to support it (CRUD + extras).
C) For each API give:
   - Endpoint path
   - Method
   - Authentication - Token Pass on headers
   - Query params
   - Request body schema (JSON)
   - Response schema (JSON) + example payloads (success + validation error + forbidden + not found)
   - Notes on pagination/sorting/filtering conventions
D) Provide a “frontend data flow” summary: which API is called on page load, on search, on create, on update, on delete, etc.

Constraints:
- Use REST (unless GraphQL is clearly better).
- Responses must be consistent (same envelope structure).
- Include bulk endpoints if the UI has bulk operations.
- Include file upload API if the UI supports category images.
- Include reorder API if there is sorting or drag-and-drop.

Now wait for my page details and then produce the full API spec.

After complete give me the output on this path: /API_Docs/Categories.md (Create new file here)
